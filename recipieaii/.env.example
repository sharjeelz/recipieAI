# Where the frontend talks to the backend.
# In dev, Vite proxies /api -> VITE_BACKEND_URL (set this to http://localhost:8000).
# In prod, set VITE_API_BASE_URL to your deployed API origin (e.g. https://api.recipyai.com).
VITE_API_BASE_URL=/api
VITE_BACKEND_URL=http://localhost:8000
